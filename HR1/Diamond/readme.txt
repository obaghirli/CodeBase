compile commad:
g++ -std=c++11 Diamond.cpp -o Diamond
run command:
./Diamond

input file:
single space between each digit
no space at the end of any rows
no endline character at the end of the last row
